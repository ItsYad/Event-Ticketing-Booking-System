# TODO Week 13: Event Management REST API Endpoints
# Akan berisi:
# - POST   /events                                      → Create Event (US1)
# - GET    /events                                      → List Published Events (US6)
# - GET    /events/{id}                                 → Event Detail (US7)
# - POST   /events/{id}/publish                         → Publish Event (US2)
# - POST   /events/{id}/cancel                          → Cancel Event (US3)
# - POST   /events/{id}/ticket-categories               → Add Ticket Category (US4)
# - PATCH  /events/{id}/ticket-categories/{cid}/disable → Disable Category (US5)
